{
    "scripts": {
        "docker:run:api": "if docker ps -a --format '{{.Names}}' | grep -Fw container_kanbapro_api; then docker stop container_kanbapro_api; docker remove container_kanbapro_api; docker image remove -f kanbapro_api; docker build --no-cache -t kanbapro_api ./api;docker run --name container_kanbapro_api -dp 127.0.0.1:8000:8000 kanbapro_api; else docker build --no-cache -t kanbapro_api ./api;docker run --name container_kanbapro_api -dp 127.0.0.1:8000:8000 kanbapro_api; fi > /dev/null"
    }
}